spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/onlinebooking_system?useSSL=false&serverTimezone=UTC&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: '123456'
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/onlinebooking_system?useSSL=false&serverTimezone=UTC&characterEncoding=utf8&allowPublicKeyRetrieval=true
      username: root
      password: '123456'
  sql:
    init:
      mode: always
      data-locations: classpath:sql/table.sql

mybatis-plus:
  type-aliases-package: org.zzu.pojo
  global-config:
    db-config:
      logic-delete-field: isDeleted
      id-type: auto

auth:
  redis:
    enabled: false
